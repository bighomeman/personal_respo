# getblacklist_function
# please keep value name consist with '.py' file name
# fun + number by order
[function_list]
funclist = ransomwaretracker , zeustracker , malwaredomains , CoinBlockerLists

# OS Windows 
[Windows_path]
data_path = .\data\
log_path  = .\logs\

# OS Linux
[Linux_path]
data_path = ./data/
log_path  = ./logs/

# begin time is setted by youself
# if set starttime = now ,it means starttime is right now.
# run period scale: s = second, m = minute, d = day
[frequency]
starttime = now
period = 5m

# Elasticsearch Server
[Elasticsearch]
server = localhost
port = 9200
